services:
  database:
    container_name: "next-boirleplate_db"
    image: "postgres:16.0-alpine3.18"
    env_file:
      - ../../.env
    ports:
      # HOST:CONTAINER (DEFAULT POSTGRES VALUE)
      - "5555:5432"
